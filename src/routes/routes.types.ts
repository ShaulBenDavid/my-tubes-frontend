export enum Routes {
  // AUTH
  LOGOUT = "/logout",
  // Public Routes
  ROOT = "/",
  LOGIN = "/login",
  GOOGLE = "/google",

  // Private Routes
  DASHBOARD = "/dashboard",
  SUBSCRIPTIONS = "/subscriptions",
  GROUP = "/group",
  // Settings
  SETTINGS = "/settings",
  ACCOUNT = "/account",
  MY_TUBES = "/mytubes",
  MY_LINKS = "/my-links",

  // Error
  NOT_FOUND = "/404",
}
