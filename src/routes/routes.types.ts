export enum Routes {
  // AUTH
  LOGOUT = "/logout",
  // Public Routes
  ROOT = "/",
  LOGIN = "/login",
  GOOGLE = "/google",
  CONTACT_US = "contact-us",
  PRIVACY_POLICY = "privacy-policy",

  // Private Routes
  DASHBOARD = "/dashboard",
  SUBSCRIPTIONS = "/subscriptions",
  GROUP = "/group",
  USERS = "/users",
  // Settings
  SETTINGS = "/settings",
  ACCOUNT = "/account",
  MY_TUBES = "/mytubes",
  MY_LINKS = "/my-links",

  // Error
  NOT_FOUND = "/404",
}
