export enum Routes {
  // AUTH
  LOGOUT = "/logout",
  // Public Routes
  ROOT = "/",
  LOGIN = "/login",
  GOOGLE = "/google",

  // Private Routes
  DASHBOARD = "/dashboard",
  SUBSCRIPTIONS = "/subscriptions",
  GROUP = "/group",

  // Error
  NOT_FOUND = "/404",
}
